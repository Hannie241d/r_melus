from django.apps import AppConfig

class DocentesConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'applications.grupos'  # El nombre de la aplicación debe coincidir exactamente
